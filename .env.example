# 1. Copy this file and rename it to .env.local.
# 2. Update the environment variables below.

# REQUIRED SETTINGS
# ################# 
# If you are using LiveKit Cloud, the API key and secret can be generated from the Cloud Dashboard.
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=
# URL pointing to the LiveKit server. (example: `wss://my-livekit-project.livekit.cloud`)
LIVEKIT_URL=

# Postgres connection string. On Coolify, create a Postgres service and use its internal connection URL.
DATABASE_URL=
# Set to true only when your Postgres connection requires TLS.
DATABASE_SSL=false

# Host console authentication.
# Generate with: openssl rand -base64 32
AUTH_SECRET=
# Bootstrap only: if admin_users is empty, the first email becomes the owner account.
HOST_ADMIN_EMAILS=admin@example.com
# Bootstrap only: temporary first-owner password. Change it from /host after first sign-in.
HOST_ADMIN_PASSWORD=
# Optional bootstrap password hash generated with lib/admin-auth.ts helpers.
# HOST_ADMIN_PASSWORD_HASH=


# OPTIONAL SETTINGS
# ################# 
# Recording
# S3_KEY_ID=
# S3_KEY_SECRET=
# S3_ENDPOINT=
# S3_BUCKET=
# S3_REGION=

# PUBLIC
# Uncomment settings menu when using a LiveKit Cloud, it'll enable Krisp noise filters.
# NEXT_PUBLIC_SHOW_SETTINGS_MENU=true
# NEXT_PUBLIC_LK_RECORD_ENDPOINT=/api/record
